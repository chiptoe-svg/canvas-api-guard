# Decisions taken during the canvas-api-authoring build

Date: 2026-09-10
Branch: canvas-api-authoring (c08ad4c..06575b6)
Plan: docs/superpowers/plans/2026-09-10-canvas-api-authoring.md

Every judgement call made without asking, in the order made. Each says what was decided,
why, and what it costs if wrong. The build ledger was scratch and is gone; this is the record.

- Ruling 1: Tasks 4-9 run sequentially, one implementer at a time, not in parallel as the plan's File Structure note suggests. Why: this skill forbids parallel implementers because they conflict, and all six append to one sources.md. Cost if wrong: wall-clock time only; the content is identical either way.
- Ruling 2: Task 6's New Quizzes stub records its pages in sources.md with the URL and fetched date but with empty `endpoints:` and `params:` lists. Why: the stub asserts no coverage, so it must claim nothing the checker would police, and the parser already accepts a block with no claims. Cost if wrong: the two New Quizzes pages go unchecked for drift, which matches the fact that nothing depends on them.
- Ruling 3: A page cited by more than one reference gets its own record under each reference's `##` block, and Task 10 Step 1's expected count is the number of records in sources.md rather than the number of distinct URLs. Why: the checker groups its report by reference file, so a shared page must appear under each file that relies on it. Cost if wrong: a handful of duplicate fetches, and a page count that reads higher than the distinct-page count.
- Ruling 4: The task-brief extractor cut Tasks 4-9 down to their page and section lists, dropping the five-part section template and the shared six-step procedure that sit above them in the plan. I prepended both, verbatim, to briefs 4 through 9 before any of them dispatched. Why: the brief must be the single source of requirements, and those six briefs were unimplementable as extracted. Cost if wrong: nothing; the prepended text is copied from the plan without change.
- Ruling 5: the reviewer's ⚠️ item is a real gap and enters the fix loop. `missing()` used a plain substring test for params while endpoints used `_appears`. I reproduced it at f37fd4a: a recorded `per_page` reads as present in a page containing only `per_page_max`, and a recorded `id` reads as present in the word "identifier". Task 4 will record exactly these bare unbracketed params (`per_page`, `include[]`, SIS id forms), so this is load-bearing, not hypothetical. A drift tool that silently prints "nothing missing" is worse than no tool. The defect is inherited from the plan's own code block, not an implementer deviation, so this ruling also amends the plan. Cost if wrong: one extra fix round on a tool nothing yet depends on.
- Ruling 6: the implementer flagged that it could not source "the plan verb changed this decision" from its four evidence files and had derived it. The claim is correct and I resolved it from git rather than sending it back: run-plan landed in c6cfbfe on 2026-09-09, one day before the file was written. The implementer simply was not given git history as evidence. Cost if wrong: a framing claim in one paragraph, contradicted by a one-line git check anyone can repeat.
- Ruling 7: the "every trap carries inline (docs:/(repo:) evidence" constraint does NOT bind Task 3, and no fix is needed. Verified: the file contains zero `**Traps.**` blocks, because that block is defined only by the five-part Canvas-reference template that Tasks 4-9 use and Task 3 does not. Task 3's content is decision criteria, not traps, and it does cite its evidence — it names `level2/README.md`, `level2/SKILL.md` and `codex/skills/canvas-api-guard/SKILL.md` inline in six places. Task 10 Step 4's check, run against this file as written, already returns 0. Cost if wrong: a reference whose sourcing is by filename in prose rather than by parenthetical marker.
- Ruling 8: `file.endpoint_attributes.html` was mislisted in the plan. It documents HTML link attributes (data-api-endpoint / data-api-returntype), not the bracket-array or include[] parameter syntax I assumed when writing the page list. The implementer remapped it to the HTML-fields section rather than forcing it. I am accepting the remapping pending the reviewer's independent fetch. include[] remains covered from courses.html and enrollments.html, where it is actually documented. Cost if wrong: one section cites a page about a different subject, caught by the reviewer's own fetch.
- Ruling 9: fundamentals.md section 8 ("a quiz is an assignment underneath") ships thinly grounded for now and is revisited after Task 6. None of Task 4's nine pages document that relationship, and the implementer correctly refused to fill it from memory. assignments.html (Task 5) and quizzes.html (Task 6) carry the real evidence. I will strengthen section 8 in the final fix wave once both are fetched. Cost if wrong: one conceptual section stays honestly scoped instead of fully cited.
- Ruling 10: Important 2 exposes a SECOND defect in the checker, which I reproduced at 01e243c. `_appears` checks only the TRAILING boundary, so `page` matches inside `per_page`, `id` inside `grid`, and `available` inside `unavailable`. It only becomes load-bearing now, because fixing Important 2 means recording short enum values and bare field names as claims. Task 2 is reopened for one round BEFORE Task 4's content fix, so that Task 4 verifies against a correct checker. Sequential, not parallel. Cost if wrong: one extra round on a tool five later tasks depend on.
- Ruling 11: the boundary check is applied per edge, conditioned on the claim's own first and last character being word characters. A claim starting with `/` skips the leading check, so a path still matches inside a full URL; a claim ending with `]` skips the trailing check. Without this, `/api/v1/...` inside `https://host/api/v1/...` would report a false MISSING. Cost if wrong: false positives, which are noisy but not silent.
- Ruling 12: the recording rule from Task 4's Finding 2 is promoted to a standing constraint for Tasks 5-9 and carried in every remaining dispatch: every parameter name, field name and enum value the prose states as fact gets a line under `params:`, not a representative sample. Why: an unrecorded claim escapes the drift check permanently, and Task 4 showed the gap appears naturally unless it is stated. Cost if wrong: larger sources.md blocks and a few more fetch comparisons per run.
- Ruling 13: my description of Finding 1 was wrong and the implementer was right to follow the page over me. I said GET /courses/:id's include[] was the list endpoint's enumeration plus three extras; the re-fetched page shows a separate, mostly non-overlapping enumeration of nine values. The implementer followed the page and flagged the discrepancy, which is exactly the instruction. Cost if wrong: none — the page is the authority and it was consulted directly.
- Ruling 14: a THIRD checker defect, reproduced by me against the live pagination page. The tool matches against raw HTML, so any claim containing a quote fails: `rel="next"` is absent from the raw bytes but present after html.unescape, because the page entity-encodes it. This is a FALSE POSITIVE, and its damage is indirect but real — it pressures an author into recording a weakened claim to get a green run, which is what happened here (bare `next` recorded instead of `rel="next"`). Task 2 gets one more round to unescape before matching, then Task 4 gets a short round to restore the faithful claims. Fixing it now spares the four remaining references the same pressure. Cost if wrong: one small round on a tool four later tasks depend on.
- Ruling 15: I settled the include[] question myself against the raw page, because three accounts conflicted (mine, the implementer's, the reviewer's) and an invented factual claim is the worst failure this skill can have. Ground truth, extracted from the live page:
- Ruling 16: the implementer's closing concern is correct and is recorded as a known limitation of the tool, not a defect. `canvas-docs-check.py` proves that each recorded claim still APPEARS on its page. It cannot prove a list is complete or exclusive, so "these 21 values and no others" is beyond what a clean run establishes. This is inherent to a substring-presence check and is the right scope for it. Cost if wrong: none; it constrains what a green run may be read to mean, and that reading belongs in the skill rather than in the tool.
- Ruling 17: root cause of Task 4's three rounds, promoted to a standing instruction for Tasks 5-9. WebFetch answers through a summarizing model, which is fine for prose and behaviour but unreliable for EXACT SET MEMBERSHIP — it is what produced the false include[] split. From here on, whenever a section asserts a complete enumeration (allowed values, enum members, question types), the implementer fetches the page with raw curl and extracts the literal list, and does not assert a relationship between two endpoints' menus without extracting both. Cost if wrong: a little more work per enumeration, against the one failure that would make this skill worse than nothing.
- Ruling 18: my dispatch was overbroad and caused a real distortion. I told Tasks 4-9 to name no local verb including `draft`, but the plan's own Step 5 grep never included `draft` — only canvas_api_guard, canvas_api_operations, --all-pages, --fields, run-plan, --dry-run. Canvas's assignments page literally reads "(Only useful if 'draft state' account setting is on)", and the implementer paraphrased it to "a named account-level setting" to satisfy my instruction. That is Canvas's own vocabulary being suppressed to protect a constraint about LOCAL tooling names, and it leaves the reader unable to act on the sentence. The constraint targets local verbs and flags, never Canvas terminology. Task 5 gets one short round to restore the literal term. Tasks 6-9 dispatches will carry the corrected constraint. Cost if wrong: one Canvas feature name appears in a file that names no local tooling — which is the plan's actual rule.
- Ruling 19: my brief overstated the documentation. I told Task 7 that rubrics.html describes the criteria shape "with ratings nested inside on the same indexed pattern". I verified: the page says only "An indexed Hash of RubricCriteria objects where the keys are integer ids and the values are the RubricCriteria objects" and NEVER shows the literal bracket form — `criteria][` occurs zero times. The implementer was right to record it as a trap rather than reconstruct it. Cost if wrong: none; the page was checked directly.
- Ruling 20: a THIRD repo-verified trap is added to this file, because it is the clearest case in the whole skill of the repo knowing something the documentation does not. `level2/canvas_api_operations.py:191` builds `criteria` as a dict keyed by `str(index)` from enumerate, with each criterion's `ratings` nested the same way, and test_canvas_api_operations.py:64-66 asserts that exact shape. So the literal working form is string-keyed sequential indices — which also RESOLVES the integer-vs-string contradiction the implementer flagged (docs say "integer ids", response examples show "_10"/"name_2", and the tested code sends "0"/"1"). The same code carries a live-verified note that Canvas stores free_form_criterion_comments false as null, observed on a real rubric. A build-side skill whose value is saving a reader from a refused write must carry this. Cost if wrong: a labelled repo-verified trap sits beside a documented one, which is the labelling scheme working as designed.
- Ruling 21: PLAN AMENDMENT to the constraint check, caused by a collision between two of my own rules. The global constraints require every trap to cite evidence as `(docs: <url>)` or `(repo: <path>)`, while the Step 5 grep forbids the literal string `canvas_api_operations`. A repo citation therefore trips a check that exists to catch CLI verb and flag names. The implementer kept the citation and flagged it rather than weakening the evidence, which was right. Amended rule: a local name is permitted ONLY on a line carrying a `repo:` evidence citation. The check becomes:
